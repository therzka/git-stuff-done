# GitHub organization(s) to filter notifications, PRs, and links (comma-separated for multiple)
GITHUB_ORG=your-org-name

# (Optional) Read-only GitHub token for API calls (fine-grained PAT recommended)
# If not set, falls back to GH_TOKEN or `gh auth token`
# Create at: https://github.com/settings/personal-access-tokens/new
# Required permissions: Issues (read), Pull requests (read), Notifications (read)
GITHUB_READ_TOKEN=

# (Optional) Directory for storing logs and TODOs (must be a git repo for auto-push)
# If not set, stores in the app directory
GIT_STUFF_DONE_DATA_DIR=~/my-work-logs
